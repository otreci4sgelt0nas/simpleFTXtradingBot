# percentage calculator by two inputs
# amount is amount, percentage is percentage 
# e.g. 5% of $120 = $140.00
#


def percetageCalculator(amount,percentage):
    x = amount - amount*percentage/100
    return x

